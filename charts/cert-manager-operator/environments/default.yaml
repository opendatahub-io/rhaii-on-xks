# User-specific configuration
#
# Auth options (set ONE):
#   useSystemPodmanAuth: true                 # Use ~/.config/containers/auth.json (persistent)
#   pullSecretFile: ~/pull-secret.txt         # Pull secret file path
#   authFile: /custom/path/auth.json          # Custom auth file path

# Use system podman auth
# Requires: cp ~/pull-secret.txt ~/.config/containers/auth.json
useSystemPodmanAuth: true

# Bundle version (uncomment to override)
# bundle:
#   version: "v1.18.0"
